services:
  # Backend Node.js Express + Socket.io service
  backend:
    image: aotms_lms-1-backend:latest
    build:
      context: ./Backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - ./Backend/.env
    restart: always
